// AMD module that bridges pre-loaded global libs (jQuery, Vue, turf) into the
// include.module loader. In the Vite/TS build these are imported directly —
// this file exists only for module-parity with the legacy AMD bundle.
export {}
