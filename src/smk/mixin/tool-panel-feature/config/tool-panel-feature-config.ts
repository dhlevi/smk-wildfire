// Mixin config defaults — AMD legacy, superseded by TS tool constructors.
export const defaults = {
    attributeMode: 'default' as string,
    command: {
        navigator:     true,
        zoom:          true,
        select:        true,
        attributeMode: false,
    },
}
export default defaults
