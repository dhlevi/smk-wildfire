// Mixin config defaults — AMD legacy, superseded by TS tool constructors.
export const defaults = {
    type:      null as string | null,
    instance:  null as string | boolean | null,
    order:     1,
    enabled:   false,
    title:     null as string | null,
    icon:      'widgets',
    showTitle: false,
}
export default defaults
