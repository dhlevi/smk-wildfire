// Tool default config — AMD legacy, not used in Vite/TS build.
export const config = {
    type:     'query',
    instance: true,
    order:    5,
    within:   false,
    command: {
        within: true,
        select: true,
    },
}
export default config
