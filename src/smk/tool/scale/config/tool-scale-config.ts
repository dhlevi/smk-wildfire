// Tool default config — AMD legacy, not used in Vite/TS build.
export const config = {
    type:       'scale',
    order:      2,
    showFactor: true,
    showBar:    true,
    showZoom:   false,
}
export default config
