// Tool default config — AMD legacy, not used in Vite/TS build.
export const config = {
    type:     'markup',
    instance: true,
    order:    3,
    position: 'toolbar',
    icon:     'edit',
    title:    'Markup',
}
export default config
