// Tool default config — AMD legacy, not used in Vite/TS build.
export const config = {
    type:     'reset-view',
    position: 'actionbar',
    order:    10,
    icon:     'zoom_out_map',
    title:    'Reset View',
}
export default config
